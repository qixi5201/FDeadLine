<?xml version="1.0" encoding="utf-8"?>
<manifest package="COM.TQC.GDD01"
          xmlns:android="http://schemas.android.com/apk/res/android">

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity android:name=".MainActivity"
        android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
      <intent-filter android:label="@string/app_name">
        <action android:name="android.intent.action.VIEW"/>

        <category android:name="android.intent.category.DEFAULT"/>
        <category android:name="android.intent.category.BROWSABLE"/>

        <data
          android:host="lndata.com"
          android:scheme="https"/>
        <data
          android:host="open.david.app"
          android:scheme="app"/>
      </intent-filter>
    </activity>
    <activity android:name=".TimeAlarmActivity"></activity>
    <activity android:name=".Area"></activity>
    <activity android:name=".Bill"></activity>
    <activity android:name=".CaptureAct"></activity>
    <activity android:name=".kind1Activity"></activity>
    <activity android:name=".new_Cost"></activity>
    <activity android:name=".Note"></activity>
    <activity android:name=".Scanner"></activity>
    <activity android:name=".Sell"></activity>
    <activity android:name=".time1Activity"></activity>
    <activity android:name=".Record"></activity>
    <activity android:name=".timeActivity"></activity>
    <activity android:name=".kindActivity"></activity>
  </application>

</manifest>